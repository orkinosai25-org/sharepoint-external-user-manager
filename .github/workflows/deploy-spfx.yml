# GitHub Actions Workflow for SharePoint Framework Solution Deployment
# 
# This workflow builds and deploys the SPFx solution to SharePoint App Catalog
# Triggered on every push to the main branch
#
# Required Repository Secrets:
# - SPO_URL: SharePoint tenant URL (e.g., https://contoso.sharepoint.com)
# - SPO_CLIENT_ID: Azure AD App Registration Client ID (Application ID)
# - SPO_CLIENT_SECRET: Azure AD App Registration Client Secret
# - SPO_TENANT_ID: Azure AD Tenant ID (optional, can be derived from SPO_URL)
#
# The workflow performs the following steps:
# 1. Build: Install dependencies, build and package the SPFx solution
# 2. Deploy: Upload .sppkg file to tenant app catalog and publish it
#
# Note: Ensure the user account has SharePoint administrator privileges
# and can access the tenant app catalog

name: Deploy SPFx Solution to SharePoint

on:
  push:
    branches: [ main ]
  workflow_dispatch: # Allow manual triggering

env:
  NODE_VERSION: '18.19.0'

jobs:
  build:
    name: Build SPFx Solution
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js ${{ env.NODE_VERSION }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: 'src/client-spfx/package-lock.json'
        
    - name: Verify Node.js version
      run: |
        echo "Current Node.js version: $(node --version)"
        echo "Current npm version: $(npm --version)"
        echo "Expected Node.js version: ${{ env.NODE_VERSION }}"
        # Verify Node.js version matches expected
        if [[ "$(node --version)" != "v${{ env.NODE_VERSION }}" ]]; then
          echo "❌ Node.js version mismatch!"
          echo "Expected: v${{ env.NODE_VERSION }}"
          echo "Actual: $(node --version)"
          exit 1
        fi
        echo "✅ Node.js version verified"
        
    - name: Install dependencies
      working-directory: src/client-spfx
      run: |
        echo "Installing npm dependencies..."
        echo "Node.js version: $(node --version)"
        echo "npm version: $(npm --version)"
        npm ci --no-optional --legacy-peer-deps
        
    - name: Build solution
      working-directory: src/client-spfx
      run: |
        echo "Building SPFx solution..."
        npm run build
        
    - name: Package solution
      working-directory: src/client-spfx
      run: |
        echo "Packaging SPFx solution for deployment..."
        npm run package-solution
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: spfx-package
        path: src/client-spfx/sharepoint/solution/*.sppkg
        retention-days: 30

  deploy:
    name: Deploy to SharePoint App Catalog
    runs-on: ubuntu-latest
    needs: build
    environment: production # Requires approval for production deployment
    
    steps:
    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: spfx-package
        path: ./solution
        
    - name: Install PnP PowerShell
      shell: pwsh
      run: |
        Write-Host "Installing PnP PowerShell module..."
        Install-Module -Name PnP.PowerShell -Force -Scope CurrentUser -AllowClobber
        Write-Host "PnP PowerShell version: $(Get-Module -Name PnP.PowerShell -ListAvailable | Select-Object -ExpandProperty Version)"
        
    - name: Validate deployment prerequisites
      shell: pwsh
      env:
        SPO_URL: ${{ secrets.SPO_URL }}
        SPO_CLIENT_ID: ${{ secrets.SPO_CLIENT_ID }}
        SPO_CLIENT_SECRET: ${{ secrets.SPO_CLIENT_SECRET }}
        SPO_TENANT_ID: ${{ secrets.SPO_TENANT_ID }}
      run: |
        # Check if required secrets are provided
        if (-not $env:SPO_URL) {
          Write-Host "❌ SPO_URL secret is not configured"
          exit 1
        }
        if (-not $env:SPO_CLIENT_ID) {
          Write-Host "❌ SPO_CLIENT_ID secret is not configured"
          exit 1
        }
        if (-not $env:SPO_CLIENT_SECRET) {
          Write-Host "❌ SPO_CLIENT_SECRET secret is not configured"
          exit 1
        }
        
        # Check if package file exists
        $packageFiles = Get-ChildItem -Path "./solution" -Filter "*.sppkg" -ErrorAction SilentlyContinue
        if (-not $packageFiles) {
          Write-Host "❌ No .sppkg file found in solution directory"
          Write-Host "Contents of solution directory:"
          Get-ChildItem -Path "./solution" -Force
          exit 1
        }
        
        Write-Host "✅ All prerequisites validated"
        
    - name: Deploy to SharePoint App Catalog
      shell: pwsh
      env:
        SPO_URL: ${{ secrets.SPO_URL }}
        SPO_CLIENT_ID: ${{ secrets.SPO_CLIENT_ID }}
        SPO_CLIENT_SECRET: ${{ secrets.SPO_CLIENT_SECRET }}
        SPO_TENANT_ID: ${{ secrets.SPO_TENANT_ID }}
      run: |
        try {
          Write-Host "=============================================="
          Write-Host "🚀 SharePoint Framework Solution Deployment"
          Write-Host "=============================================="
          Write-Host "Deployment Started: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss UTC')"
          Write-Host "SharePoint URL: $env:SPO_URL"
          Write-Host "Client ID: $env:SPO_CLIENT_ID"
          Write-Host "Workflow Run: ${{ github.run_number }}"
          Write-Host "Commit SHA: ${{ github.sha }}"
          Write-Host ""
          
          Write-Host "🔗 Connecting to SharePoint Online using Azure AD App..."
          
          # Extract tenant name from SharePoint URL if tenant ID not provided
          $tenantId = $env:SPO_TENANT_ID
          if (-not $tenantId) {
            # Extract tenant name from URL
            # Supports: https://contoso.sharepoint.com, https://contoso-admin.sharepoint.com
            # Note: Custom domains or non-standard URLs may require explicit SPO_TENANT_ID
            if ($env:SPO_URL -match 'https://([^.]+?)(-admin)?\.sharepoint\.com') {
              $tenantName = $matches[1]
              Write-Host "Detected tenant name from URL: $tenantName"
              Write-Host "Note: If connection fails, set SPO_TENANT_ID secret explicitly"
            } else {
              throw "Could not extract tenant name from SPO_URL ($env:SPO_URL). Please set SPO_TENANT_ID repository secret explicitly."
            }
          }
          
          # Connect to SharePoint Online with retry logic using modern authentication
          $maxRetries = 3
          $retryCount = 0
          $connected = $false
          
          while (-not $connected -and $retryCount -lt $maxRetries) {
            try {
              # Use modern authentication with client ID and secret
              if ($tenantId) {
                Write-Host "Connecting with Tenant ID: $tenantId"
                Connect-PnPOnline -Url $env:SPO_URL -ClientId $env:SPO_CLIENT_ID -ClientSecret $env:SPO_CLIENT_SECRET -Tenant $tenantId
              } else {
                # Try with .onmicrosoft.com suffix (standard Microsoft 365 tenant)
                Write-Host "Connecting with derived tenant: $tenantName.onmicrosoft.com"
                Connect-PnPOnline -Url $env:SPO_URL -ClientId $env:SPO_CLIENT_ID -ClientSecret $env:SPO_CLIENT_SECRET -Tenant "$tenantName.onmicrosoft.com"
              }
              $connected = $true
              Write-Host "✅ Successfully connected to SharePoint Online"
            } catch {
              $retryCount++
              Write-Host "⚠️ Connection attempt $retryCount failed: $($_.Exception.Message)"
              if ($retryCount -lt $maxRetries) {
                Write-Host "Retrying in 10 seconds..."
                Start-Sleep -Seconds 10
              }
            }
          }
          
          if (-not $connected) {
            throw "Failed to connect to SharePoint Online after $maxRetries attempts"
          }
          
          # Find the .sppkg file
          $packageFile = Get-ChildItem -Path "./solution" -Filter "*.sppkg" | Select-Object -First 1
          
          if (-not $packageFile) {
            throw "No .sppkg file found in solution directory"
          }
          
          Write-Host "Found package file: $($packageFile.Name)"
          Write-Host "Package size: $([math]::Round($packageFile.Length / 1MB, 2)) MB"
          
          # Check if app already exists and get info
          $existingApp = $null
          try {
            $allApps = Get-PnPApp
            $existingApp = $allApps | Where-Object { $_.Title -eq "sharepoint-external-user-manager-client-side-solution" }
            if ($existingApp) {
              Write-Host "Existing app found: $($existingApp.Title) (Version: $($existingApp.AppVersion))"
            }
          } catch {
            Write-Host "Could not retrieve existing apps list: $($_.Exception.Message)"
          }
          
          # Upload and publish the app
          Write-Host "📦 Uploading package to app catalog..."
          Write-Host "Target: $env:SPO_URL App Catalog"
          $uploadStartTime = Get-Date
          $app = Add-PnPApp -Path $packageFile.FullName -Publish -Overwrite
          $uploadDuration = (Get-Date) - $uploadStartTime
          
          if ($app) {
            Write-Host ""
            Write-Host "=============================================="
            Write-Host "✅ DEPLOYMENT SUCCESSFUL"
            Write-Host "=============================================="
            Write-Host "Package: $($packageFile.Name)"
            Write-Host "Package Size: $([math]::Round($packageFile.Length / 1MB, 2)) MB"
            Write-Host "App ID: $($app.Id)"
            Write-Host "App Title: $($app.Title)"
            Write-Host "App Version: $($app.AppVersion)"
            Write-Host "Upload Duration: $($uploadDuration.TotalSeconds) seconds"
            Write-Host "Deployed to: $env:SPO_URL"
            Write-Host "Deployment Completed: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss UTC')"
            Write-Host "=============================================="
            
            # Set deployment summary
            $deploymentSummary = @"
        ## 🚀 SPFx Solution Deployment Summary
        
        **Status**: ✅ **SUCCESSFUL**
        
        ### Deployment Details
        - **Package**: $($packageFile.Name)
        - **Package Size**: $([math]::Round($packageFile.Length / 1MB, 2)) MB
        - **App ID**: $($app.Id)
        - **App Title**: $($app.Title)
        - **App Version**: $($app.AppVersion)
        - **Upload Duration**: $($uploadDuration.TotalSeconds) seconds
        - **Deployed to**: $env:SPO_URL
        - **Workflow Run**: ${{ github.run_number }}
        - **Commit SHA**: ${{ github.sha }}
        - **Deployment Date**: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss UTC')
        
        ### SharePoint App Catalog
        The SPFx solution has been successfully uploaded to the SharePoint App Catalog at **$env:SPO_URL** and is ready for use.
        
        ### Next Steps
        1. 🔍 Navigate to your SharePoint App Catalog to verify the deployment
        2. ✅ The app is automatically published and available for installation
        3. 🎯 Add the web part to SharePoint pages as needed
        4. 📋 The solution includes multiple web parts: External User Manager, Meeting Room Booking, Inventory Product Catalog, Timesheet Management, and AI-Powered FAQ
        
        ### Deployment Logs
        Check the workflow logs above for detailed deployment information and any additional output.
        "@
            
            Write-Host $deploymentSummary
            
            # Add to GitHub step summary
            Add-Content -Path $env:GITHUB_STEP_SUMMARY -Value $deploymentSummary
            
          } else {
            throw "Failed to deploy package to app catalog - no app object returned"
          }
          
        } catch {
          Write-Host ""
          Write-Host "=============================================="
          Write-Host "❌ DEPLOYMENT FAILED"
          Write-Host "=============================================="
          Write-Host "Error: $($_.Exception.Message)"
          Write-Host "Workflow Run: ${{ github.run_number }}"
          Write-Host "Commit SHA: ${{ github.sha }}"
          Write-Host "Failed at: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss UTC')"
          Write-Host "SharePoint URL: $env:SPO_URL"
          Write-Host "=============================================="
          
          $errorSummary = @"
        ## ❌ SPFx Solution Deployment Failed
        
        **Status**: ❌ **FAILED**
        
        ### Error Details
        - **Error**: $($_.Exception.Message)
        - **SharePoint URL**: $env:SPO_URL
        - **Workflow Run**: ${{ github.run_number }}
        - **Commit SHA**: ${{ github.sha }}
        - **Failed at**: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss UTC')
        
        ### Troubleshooting
        1. 🔍 Check the workflow logs above for detailed error information
        2. 🔐 Verify that repository secrets (SPO_URL, SPO_CLIENT_ID, SPO_CLIENT_SECRET) are correctly configured
        3. 📋 Ensure the Azure AD App Registration has the required SharePoint permissions
        4. 🔑 Verify the Azure AD App has Sites.FullControl.All application permission
        5. 🌐 Confirm that the SharePoint tenant URL is accessible and correct
        6. 🔄 Try running the deployment again if this was a temporary issue
        
        ### Repository Secrets Required
        - **SPO_URL**: SharePoint tenant URL (e.g., https://contoso.sharepoint.com)
        - **SPO_CLIENT_ID**: Azure AD App Registration Client ID (Application ID)
        - **SPO_CLIENT_SECRET**: Azure AD App Registration Client Secret
        - **SPO_TENANT_ID**: (Optional) Azure AD Tenant ID - if not provided, will be derived from SPO_URL
        
        ### Azure AD App Registration Setup
        1. Register a new app in Azure AD (or use existing one)
        2. Grant API permissions: SharePoint Sites.FullControl.All (Application permission)
        3. Grant admin consent for the permissions
        4. Create a client secret and save it securely
        5. Add the Client ID and Secret to repository secrets
        "@
          
          Write-Host $errorSummary
          Add-Content -Path $env:GITHUB_STEP_SUMMARY -Value $errorSummary
          
          exit 1
        } finally {
          # Disconnect from SharePoint
          try {
            Disconnect-PnPOnline
            Write-Host "Disconnected from SharePoint Online"
          } catch {
            Write-Host "Warning: Could not disconnect cleanly from SharePoint"
          }
        }