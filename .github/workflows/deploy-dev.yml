name: Deploy to Dev Environment

on:
  push:
    branches: [ develop ]
    paths:
      - 'src/**'
      - 'infra/bicep/**'
      - '.github/workflows/deploy-dev.yml'
  workflow_dispatch:
    inputs:
      deploy_infrastructure:
        description: 'Deploy infrastructure (Bicep templates)'
        required: false
        type: boolean
        default: false

env:
  DOTNET_VERSION: '8.0.x'
  NODE_VERSION: '18.19.0'
  AZURE_RESOURCE_GROUP: 'spexternal-dev-rg'
  ENVIRONMENT: 'dev'

permissions:
  contents: read

jobs:
  # Job 1: Build all components
  build:
    name: Build All Components
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    # Build API
    - name: Setup .NET ${{ env.DOTNET_VERSION }}
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
        
    - name: Build API
      run: |
        cd src/api-dotnet/WebApi/SharePointExternalUserManager.Api
        dotnet restore
        dotnet build --configuration Release --no-restore
        dotnet publish --configuration Release --output ../../../../api-publish --no-build
        
    - name: Validate Azure AD Secrets
      env:
        AZURE_AD_CLIENT_ID: ${{ secrets.AZURE_AD_CLIENT_ID }}
        AZURE_AD_CLIENT_SECRET: ${{ secrets.AZURE_AD_CLIENT_SECRET }}
        AZURE_AD_TENANT_ID: ${{ secrets.AZURE_AD_TENANT_ID }}
      run: |
        errors=""
        
        if [ -z "$AZURE_AD_CLIENT_ID" ]; then
          errors="$errors AZURE_AD_CLIENT_ID"
        fi
        if [ -z "$AZURE_AD_CLIENT_SECRET" ]; then
          errors="$errors AZURE_AD_CLIENT_SECRET"
        fi
        if [ -z "$AZURE_AD_TENANT_ID" ]; then
          errors="$errors AZURE_AD_TENANT_ID"
        fi
        
        if [ -n "$errors" ]; then
          echo "❌ Required Azure AD secrets are not configured:$errors"
          echo ""
          echo "ℹ️ To fix this:"
          echo "  1. Go to repository Settings > Secrets and variables > Actions"
          echo "  2. Add the following secrets:"
          echo "     - AZURE_AD_CLIENT_ID: Your Azure AD Application Client ID"
          echo "     - AZURE_AD_CLIENT_SECRET: Your Azure AD Application Client Secret"
          echo "     - AZURE_AD_TENANT_ID: Your Azure AD Tenant ID"
          echo "  3. See WORKFLOW_SECRET_SETUP.md for detailed instructions"
          exit 1
        fi
        
        echo "✅ All Azure AD secrets validated"
    
    - name: Create Development Configuration for Portal
      env:
        AZURE_AD_CLIENT_ID: ${{ secrets.AZURE_AD_CLIENT_ID }}
        AZURE_AD_CLIENT_SECRET: ${{ secrets.AZURE_AD_CLIENT_SECRET }}
        AZURE_AD_TENANT_ID: ${{ secrets.AZURE_AD_TENANT_ID }}
      run: |
        # Create appsettings.Production.json with secrets from repository
        cat > src/portal-blazor/SharePointExternalUserManager.Portal/appsettings.Production.json << EOF
        {
          "AzureAd": {
            "ClientId": "$AZURE_AD_CLIENT_ID",
            "ClientSecret": "$AZURE_AD_CLIENT_SECRET",
            "TenantId": "$AZURE_AD_TENANT_ID"
          }
        }
        EOF
        echo "✅ Development configuration created with secrets from repository"
    
    - name: Build Blazor Portal
      run: |
        cd src/portal-blazor/SharePointExternalUserManager.Portal
        dotnet restore
        dotnet build --configuration Release --no-restore
        dotnet publish --configuration Release --output ../../../../portal-publish --no-build
        
    # Build SPFx
    - name: Setup Node.js ${{ env.NODE_VERSION }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: 'src/client-spfx/package-lock.json'
        
    - name: Build SPFx Client
      run: |
        cd src/client-spfx
        npm ci --no-optional --legacy-peer-deps
        npm run build
        npm run package-solution
        
    # Upload artifacts
    - name: Upload API artifact
      uses: actions/upload-artifact@v4
      with:
        name: api-package
        path: api-publish
        retention-days: 1
        
    - name: Upload Portal artifact
      uses: actions/upload-artifact@v4
      with:
        name: portal-package
        path: portal-publish
        retention-days: 1
        
    - name: Upload SPFx artifact
      uses: actions/upload-artifact@v4
      with:
        name: spfx-package
        path: src/client-spfx/sharepoint/solution/*.sppkg
        retention-days: 1
        
  # Job 2: Deploy Infrastructure (optional, manual trigger only)
  deploy-infrastructure:
    name: Deploy Azure Infrastructure
    runs-on: ubuntu-latest
    # Only run if workflow was manually triggered with deploy_infrastructure=true
    if: github.event.inputs.deploy_infrastructure == 'true'
    environment: development
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Validate Azure credentials
      env:
        AZURE_CREDENTIALS: ${{ secrets.AZURE_CREDENTIALS }}
      run: |
        if [ -z "$AZURE_CREDENTIALS" ]; then
          echo "❌ AZURE_CREDENTIALS secret is not configured"
          echo "ℹ️ To enable infrastructure deployment:"
          echo "  1. Go to repository Settings > Secrets and variables > Actions"
          echo "  2. Add AZURE_CREDENTIALS secret with Azure service principal credentials"
          exit 1
        fi
        echo "✅ Azure credentials validated"
      
    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
        
    - name: Deploy Bicep template
      run: |
        az deployment group create \
          --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
          --template-file infra/bicep/main.bicep \
          --parameters environment=${{ env.ENVIRONMENT }} \
          --parameters sqlAdminUsername=${{ secrets.SQL_ADMIN_USERNAME }} \
          --parameters sqlAdminPassword=${{ secrets.SQL_ADMIN_PASSWORD }}
          
    - name: Get deployment outputs
      id: deployment
      run: |
        outputs=$(az deployment group show \
          --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
          --name main \
          --query properties.outputs)
        echo "Deployment outputs:"
        echo "$outputs"
        
    - name: Logout from Azure
      run: az logout
      
  # Job 3: Deploy API
  deploy-api:
    name: Deploy API to Azure
    runs-on: ubuntu-latest
    needs: build
    
    steps:
    - name: Download API artifact
      uses: actions/download-artifact@v4
      with:
        name: api-package
        path: ./api-package
        
    - name: Validate deployment prerequisites
      env:
        API_PUBLISH_PROFILE: ${{ secrets.API_PUBLISH_PROFILE }}
      run: |
        if [ -z "$API_PUBLISH_PROFILE" ]; then
          echo "❌ API deployment skipped - API_PUBLISH_PROFILE secret not configured"
          echo ""
          echo "ℹ️ To enable API deployment:"
          echo "  1. Go to Azure Portal → Your API App Service"
          echo "  2. Click 'Get publish profile' in Overview"
          echo "  3. Copy the entire XML content"
          echo "  4. Go to repository Settings > Secrets and variables > Actions"
          echo "  5. Add secret named 'API_PUBLISH_PROFILE' with the XML content"
          exit 1
        fi
        echo "✅ Deployment prerequisites validated"
        
    - name: Deploy to Azure App Service
      uses: azure/webapps-deploy@v3
      with:
        app-name: 'spexternal-api-dev'  # Can be overridden by publish profile
        package: ./api-package
        publish-profile: ${{ secrets.API_PUBLISH_PROFILE }}
      
  # Job 4: Deploy Blazor Portal
  deploy-portal:
    name: Deploy Blazor Portal to Azure
    runs-on: ubuntu-latest
    needs: build
    
    steps:
    - name: Download Portal artifact
      uses: actions/download-artifact@v4
      with:
        name: portal-package
        path: ./portal-package
        
    - name: Validate deployment prerequisites
      env:
        PORTAL_PUBLISH_PROFILE: ${{ secrets.PORTAL_PUBLISH_PROFILE }}
      run: |
        if [ -z "$PORTAL_PUBLISH_PROFILE" ]; then
          echo "❌ Portal deployment skipped - PORTAL_PUBLISH_PROFILE secret not configured"
          echo ""
          echo "ℹ️ To enable Portal deployment:"
          echo "  1. Go to Azure Portal → Your Portal App Service"
          echo "  2. Click 'Get publish profile' in Overview"
          echo "  3. Copy the entire XML content"
          echo "  4. Go to repository Settings > Secrets and variables > Actions"
          echo "  5. Add secret named 'PORTAL_PUBLISH_PROFILE' with the XML content"
          exit 1
        fi
        echo "✅ Deployment prerequisites validated"
        
    - name: Deploy to Azure App Service
      uses: azure/webapps-deploy@v3
      with:
        app-name: 'spexternal-portal-dev'  # Can be overridden by publish profile
        package: ./portal-package
        publish-profile: ${{ secrets.PORTAL_PUBLISH_PROFILE }}
      
  # Job 5: Health Check
  health-check:
    name: Health Check
    runs-on: ubuntu-latest
    needs: [deploy-api, deploy-portal]
    # Run if either deploy job completed (success, failure, or skipped)
    if: always()
    
    steps:
    - name: Check Deployment Status
      run: |
        API_STATUS="${{ needs.deploy-api.result }}"
        PORTAL_STATUS="${{ needs.deploy-portal.result }}"
        
        echo "## 🚀 Dev Environment Deployment Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Environment**: Development" >> $GITHUB_STEP_SUMMARY
        echo "**Deployment Date**: $(date -u +"%Y-%m-%d %H:%M:%S UTC")" >> $GITHUB_STEP_SUMMARY
        echo "**Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Deployment Status" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        # API Status
        if [ "$API_STATUS" = "success" ]; then
          echo "- ✅ API App Service: Deployed Successfully" >> $GITHUB_STEP_SUMMARY
        elif [ "$API_STATUS" = "failure" ]; then
          echo "- ❌ API App Service: Deployment Failed" >> $GITHUB_STEP_SUMMARY
        else
          echo "- ⚠️ API App Service: Skipped (API_PUBLISH_PROFILE not configured)" >> $GITHUB_STEP_SUMMARY
        fi
        
        # Portal Status
        if [ "$PORTAL_STATUS" = "success" ]; then
          echo "- ✅ Blazor Portal: Deployed Successfully" >> $GITHUB_STEP_SUMMARY
        elif [ "$PORTAL_STATUS" = "failure" ]; then
          echo "- ❌ Blazor Portal: Deployment Failed" >> $GITHUB_STEP_SUMMARY
        else
          echo "- ⚠️ Blazor Portal: Skipped (PORTAL_PUBLISH_PROFILE not configured)" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "- ✅ SPFx Package: Built Successfully" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Next Steps" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [ "$API_STATUS" = "skipped" ] || [ "$PORTAL_STATUS" = "skipped" ]; then
          echo "**Configuration Required:**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "$API_STATUS" = "skipped" ]; then
            echo "1. Configure \`API_PUBLISH_PROFILE\` secret to enable API deployment" >> $GITHUB_STEP_SUMMARY
          fi
          if [ "$PORTAL_STATUS" = "skipped" ]; then
            echo "2. Configure \`PORTAL_PUBLISH_PROFILE\` secret to enable Portal deployment" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "See [deployment documentation](../docs/DEPLOYMENT.md) for setup instructions." >> $GITHUB_STEP_SUMMARY
        else
          echo "1. Verify API endpoints are responding correctly" >> $GITHUB_STEP_SUMMARY
          echo "2. Verify Portal is accessible and functioning" >> $GITHUB_STEP_SUMMARY
          echo "3. Deploy SPFx package to SharePoint App Catalog" >> $GITHUB_STEP_SUMMARY
        fi
